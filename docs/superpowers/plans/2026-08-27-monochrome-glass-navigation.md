# Monochrome glass navigation implementation plan

1. Add focused tests for support request construction, download platform definitions, and gradient-free live chart markup.
2. Replace the global header with the approved portal-first responsive navigation and explicit authentication action.
3. Reuse the circuit hero artwork, convert the landing page and chart to a strict black-and-white glass system, and remove gradient-based styling.
4. Add authenticated support submission backed by `/v1/reports` and add the public versions/download page backed by existing release data.
5. Redirect legacy download/changelog routes, update discovery metadata, and verify accessibility and reduced-motion behavior.
6. Run tests, typecheck, build, secret scan, deploy to Cloudflare, and smoke-test the live site.
