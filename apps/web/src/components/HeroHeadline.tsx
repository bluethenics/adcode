"use client";

/** The complete promise remains readable with animation disabled. */
export function HeroHeadline() {
  return (
    <h1 className="hero-title">
      Earn while you <span className="hero-title-accent">code</span><span className="sr-only">.</span>
    </h1>
  );
}
