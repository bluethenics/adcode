import type { Tab } from "@/components/AppShell";

/** Shared so every portal page highlights the same set. */
export const PORTAL_TABS: Tab[] = [
  { href: "/portal", label: "Campaigns" },
  { href: "/portal/billing", label: "Billing" },
];
